{
	"config": {
        "vendor-dir": "plugin"
    },
    "scripts": {
		"post-update-cmd": [
			"./bin/composer.clean"
		],
		"post-install-cmd": [
			"./bin/composer.clean"
		]
	},
	"repositories":[
        {
            "type": "vcs",
            "url": "git@github.com:Triplkrypl/Kina-Base-Vhost.git"
        }
    ],
    "require":{
        "Base":"*"
    }
}